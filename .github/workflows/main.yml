name: Run simple steps

on:
  push:
    branches:
      - master
      - develo

jobs:
 call-build-workflow:
   uses: Chetan77-hub/cloud7-devops-workflows/.github/workflows/frontend.yml@master
 
      

  
