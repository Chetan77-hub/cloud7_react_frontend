name: Run simple steps

on:
  push:
    branches:
      - master
      - develop

jobs:
 call-build-workflow:
   uses: Chetan77-hub/cloud7-devops-workflows/.github/workflows/cicd/frontend.yml@master
 
      

  
